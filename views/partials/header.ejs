<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Community Posts - Home</title>

</head>

<body>
    <nav>
        <div class="nav-container">
            <div class="logo" onclick="showAuthPopup()"> BlogShare</div>
            <div class="nav-links">
                <a href="/">Home</a>

                <a onclick="showAuthPopup()">Explore</a>
                <a onclick="showAuthPopup()">Trending</a>
                <a onclick="showAuthPopup()">About</a>
            </div>
            <div class="auth-buttons">
                <% if (!user) { %>
                    <!-- Not logged in -->
                    <button class="btn btn-login" onclick="window.location.href='/login'">Login</button>
                    <button class="btn btn-register" onclick="window.location.href='/register'">Sign Up</button>
                    <% } else { %>
                        <!-- Logged in, show OTP and logout -->
                        <span>Welcome, <%= user.name %></span>
                        <button class="btn btn-otp" onclick="showOtpModal()">OTP</button>
                        <button class="btn btn-logout" onclick="window.location.href='/logout'">Logout</button>
                        <% } %>
            </div>

    </nav>