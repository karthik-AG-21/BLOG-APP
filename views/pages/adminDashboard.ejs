<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - BlogShare</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f5f7fa;
        }

        /* Sidebar */
        .sidebar {
            position: fixed;
            left: 0;
            top: 0;
            width: 280px;
            height: 100vh;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 2rem 0;
            overflow-y: auto;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
            z-index: 1000;
        }

        .sidebar-header {
            padding: 0 1.5rem 2rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
            margin-bottom: 2rem;
        }

        .sidebar-logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: white;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
        }

        .sidebar-menu {
            list-style: none;
        }

        .sidebar-menu li {
            margin: 0.5rem 0;
        }

        .sidebar-menu a {
            display: block;
            padding: 1rem 1.5rem;
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            transition: all 0.3s;
            border-left: 4px solid transparent;
            cursor: pointer;
        }

        .sidebar-menu a:hover,
        .sidebar-menu a.active {
            background: rgba(255, 255, 255, 0.1);
            border-left-color: white;
            color: white;
        }

        /* Main Content */
        .main-content {
            margin-left: 280px;
            padding: 2rem;
        }

        .header {
            background: white;
            padding: 1.5rem 2rem;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
        }

        .header h1 {
            color: #333;
            font-size: 2rem;
        }

        /* Stats Cards */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-bottom: 3rem;
        }

        .stat-card {
            background: white;
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
            border-top: 4px solid;
            transition: all 0.3s;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.12);
        }

        .stat-card.users {
            border-top-color: #667eea;
        }

        .stat-card.posts {
            border-top-color: #764ba2;
        }

        .stat-card.comments {
            border-top-color: #f093fb;
        }

        .stat-value {
            font-size: 2.5rem;
            font-weight: bold;
            color: #333;
            margin: 1rem 0;
        }

        .stat-label {
            color: #888;
            font-size: 0.95rem;
        }

        .stat-icon {
            font-size: 2rem;
        }

        /* Table Container */
        .content-section {
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
            padding: 2rem;
            margin-bottom: 2rem;
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid #f0f0f0;
        }

        .section-header h2 {
            color: #333;
            font-size: 1.5rem;
        }

        .btn {
            padding: 0.7rem 1.5rem;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s;
            text-decoration: none;
            display: inline-block;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
        }

        .btn-danger {
            background: #ff6b6b;
            color: white;
            padding: 0.5rem 1rem;
            font-size: 0.9rem;
        }

        .btn-danger:hover {
            background: #ff5252;
        }

        .btn-edit {
            background: #4ecdc4;
            color: white;
            padding: 0.5rem 1rem;
            font-size: 0.9rem;
        }

        .btn-edit:hover {
            background: #45b7aa;
        }

        .btn-cancel {
            background: #e9ecef;
            color: #333;
        }

        .btn-cancel:hover {
            background: #dee2e6;
        }

        /* Table Styles */
        table {
            width: 100%;
            border-collapse: collapse;
        }

        th {
            background: #f8f9fa;
            padding: 1rem;
            text-align: left;
            color: #333;
            font-weight: 600;
            border-bottom: 2px solid #e0e0e0;
        }

        td {
            padding: 1rem;
            border-bottom: 1px solid #f0f0f0;
            color: #666;
        }

        tr:hover {
            background: #f8f9fa;
        }

        .badge {
            display: inline-block;
            padding: 0.4rem 0.8rem;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
        }

        .badge-success {
            background: #d4edda;
            color: #155724;
        }

        .action-buttons {
            display: flex;
            gap: 0.5rem;
        }

        /* Modal Styles */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(5px);
            z-index: 2000;
            align-items: center;
            justify-content: center;
            padding: 1rem;
            overflow-y: auto;
        }

        .modal.active {
            display: flex;
        }

        .modal-content {
            background: white;
            border-radius: 25px;
            width: 100%;
            max-width: 600px;
            display: flex;
            flex-direction: column;
            position: relative;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
            animation: slideUp 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            overflow: hidden;
            margin: auto;
        }

        @keyframes slideUp {
            from {
                transform: translateY(50px);
                opacity: 0;
            }

            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .modal-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
            padding: 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: white;
            flex-shrink: 0;
        }

        .modal-header h2 {
            font-size: 1.8rem;
            margin: 0;
        }

        .close-btn {
            background: rgba(255, 255, 255, 0.9);
            color: #333;
            border: none;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            font-size: 1.5rem;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            flex-shrink: 0;
        }

        .close-btn:hover {
            background: white;
            transform: rotate(90deg) scale(1.1);
        }

        .modal-body {
            flex: 1;
            overflow-y: auto;
            padding: 2rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group label {
            display: block;
            font-weight: 600;
            color: #333;
            margin-bottom: 0.5rem;
            font-size: 1rem;
        }

        .form-group input[type="text"],
        .form-group textarea {
            width: 100%;
            padding: 0.8rem;
            border: 2px solid #e9ecef;
            border-radius: 12px;
            font-family: inherit;
            font-size: 1rem;
            transition: all 0.3s;
        }

        .form-group input[type="text"]:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .form-group textarea {
            resize: vertical;
            min-height: 120px;
        }

        .modal-footer {
            padding: 1.5rem 2rem;
            display: flex;
            gap: 1rem;
            justify-content: flex-end;
            border-top: 2px solid #e9ecef;
            flex-shrink: 0;
        }

        .empty-state {
            text-align: center;
            padding: 3rem;
            color: #888;
        }

        .empty-state-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        @media (max-width: 768px) {
            .sidebar {
                width: 70px;
            }

            .main-content {
                margin-left: 70px;
                padding: 1rem;
            }

            .stats-grid {
                grid-template-columns: 1fr;
            }

            table {
                font-size: 0.9rem;
            }

            td,
            th {
                padding: 0.5rem;
            }

            .modal-content {
                max-width: 95vw;
            }
        }
    </style>
</head>

<body>
    <!-- Sidebar Navigation -->
    <div class="sidebar">
        <div class="sidebar-header">
            <div class="sidebar-logo">📊 BlogShare Admin</div>
        </div>
        <ul class="sidebar-menu">
            <li><a href="#" onclick="showSection('dashboard', event)" class="active">📈 Dashboard</a></li>
            <li><a href="#" onclick="showSection('posts', event)">📝 Posts</a></li>
            <li><a href="#" onclick="showSection('users', event)">👥 Users</a></li>
            <li><a href="#" onclick="showSection('comments', event)">💬 Comments</a></li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Header -->
        <div class="header">
            <h1 id="page-title">Dashboard</h1>
        </div>

        <!-- Dashboard Section -->
        <div id="dashboard-section" class="section-content">
            <div class="stats-grid">
                <div class="stat-card users">
                    <div class="stat-icon">👥</div>
                    <div class="stat-label">Total Users</div>
                    <div class="stat-value">
                        <%= totalUsers || 0 %>
                    </div>
                </div>
                <div class="stat-card posts">
                    <div class="stat-icon">📝</div>
                    <div class="stat-label">Total Posts</div>
                    <div class="stat-value">
                        <%= totalPosts || 0 %>
                    </div>
                </div>
                <div class="stat-card comments">
                    <div class="stat-icon">💬</div>
                    <div class="stat-label">Total Comments</div>
                    <div class="stat-value">
                        <%= totalComments || 0 %>
                    </div>
                </div>
            </div>
        </div>

        <!-- Posts Management Section -->
        <div id="posts-section" class="section-content" style="display: none;">
            <div class="content-section">
                <div class="section-header">
                    <h2>Manage Posts</h2>

                </div>

                <% if (posts && posts.length> 0) { %>
                    <table>
                        <thead>
                            <tr>
                                <th>Title</th>
                                <th>Author</th>
                                <th>Date</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% posts.forEach(function(post) { %>
                                <tr>
                                    <td><strong>
                                            <%= post.title %>
                                        </strong></td>
                                    <td>
                                        <%= post.userId && post.userId.name ? post.userId.name : "Unknown User" %>
                                    </td>
                                    <td>
                                        <%= post.createdAt ? new Date(post.createdAt).toLocaleDateString('en-IN')
                                            : 'N/A' %>
                                    </td>
                                    <td>
                                        <div class="action-buttons">
                                            <button class="btn btn-edit" onclick="openEditPostModal('<%= post._id %>', {
                                                title: '<%= post.title.replace(/'/g, " \\'") %>',
                                                description: '<%= post.description.replace(/'/g, "\\'" ) %>',
                                                    image: '<%= (post.image || '' ).replace(/\\/g, '/' ) %>'
                                                        })">✏️ Edit</button>
                                            <button class="btn btn-danger"
                                                onclick="confirmDeletePost('<%= post._id %>', '<%= post.title %>')">🗑️
                                                Delete</button>
                                        </div>
                                    </td>
                                </tr>
                                <% }); %>
                        </tbody>
                    </table>
                    <% } else { %>
                        <div class="empty-state">
                            <div class="empty-state-icon">📭</div>
                            <p>No posts found</p>
                        </div>
                        <% } %>
            </div>
        </div>

        <!-- Users Management Section -->
        <div id="users-section" class="section-content" style="display: none;">
            <div class="content-section">
                <div class="section-header">
                    <h2>Manage Users</h2>
                </div>

                <% if (users && users.length> 0) { %>
                    <table>
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Email</th>
                                <th>Joined</th>
                                <th>Role</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% users.forEach(function(user) { %>
                                <tr>
                                    <td><strong>
                                            <%= user.name %>
                                        </strong></td>
                                    <td>
                                        <%= user.email %>
                                    </td>
                                    <td>
                                        <%= user.createdAt ? new Date(user.createdAt).toLocaleDateString('en-IN')
                                            : 'N/A' %>
                                    </td>
                                    <td><span class="badge badge-success">
                                            <%= user.role || 'User' %>
                                        </span></td>
                                    <td>
                                        <div class="action-buttons">
                                            <button class="btn btn-danger"
                                                onclick="confirmDeleteUser('<%= user._id %>', '<%= user.name %>')">🗑️
                                                Delete</button>
                                        </div>
                                    </td>
                                </tr>
                                <% }); %>
                        </tbody>
                    </table>
                    <% } else { %>
                        <div class="empty-state">
                            <div class="empty-state-icon">👨‍🦱</div>
                            <p>No users found</p>
                        </div>
                        <% } %>
            </div>
        </div>
    </div>


    <div id="comments-section" class="section-content" style="display: none;">
        <div class="content-section">
            <div class="section-header">
                <h2>Manage Comments</h2>
            </div>

            <% if (comments && comments.length> 0) { %>
                <table>
                    <thead>
                        <tr>
                            <th>Author</th>
                            <th>Comment</th>
                            <th>Post</th>
                            <th>Date</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% comments.forEach(function(comment) { %>
                            <tr>
                                <td><strong>
                                        <%= comment.author %>
                                    </strong></td>
                                <td>
                                    <%= comment.text %>
                                </td>
                                <td>
                                    <%= comment.post ? comment.post.title : 'N/A' %>
                                </td>
                                <td>
                                    <%= comment.createdAt ? new Date(comment.createdAt).toLocaleDateString('en-IN')
                                        : 'N/A' %>
                                </td>
                                <td>
                                    <div class="action-buttons">
                                        <button class="btn btn-danger"
                                            onclick="confirmDeleteComment('<%= comment._id %>', '<%= comment.author %>')">🗑️
                                            Delete</button>
                                    </div>
                                </td>
                            </tr>
                            <% }); %>
                    </tbody>
                </table>
                <% } else { %>
                    <div class="empty-state">
                        <div class="empty-state-icon">💬</div>
                        <p>No comments found</p>
                    </div>
                    <% } %>
        </div>
    </div>

    <!-- COPY THIS ENTIRE MODAL AND REPLACE YOUR EDIT MODAL -->

    <!-- Edit Post Modal -->
    <div class="modal" id="editPostModal">
        <div class="modal-content">
            <div class="modal-header">
                <h2>✏️ Edit Post</h2>
                <button class="close-btn" onclick="closeEditPostModal()">✕</button>
            </div>

            <form id="editPostForm" enctype="multipart/form-data" method="POST" action="">
                <input type="hidden" id="postIdInput" name="postId" value="">
                <input type="hidden" name="_method" value="PUT">

                <div class="modal-body">
                    <!-- Title Input -->
                    <div class="form-group">
                        <label for="editPostTitle">
                            <span style="color: #667eea; font-weight: 700;">📝</span>
                            Post Title
                            <span style="color: #f5576c; font-size: 0.9rem;">*</span>
                        </label>
                        <input type="text" id="editPostTitle" name="title" placeholder="Enter post title" required
                            maxlength="30" />
                        <small style="color: #999; display: block; margin-top: 0.3rem;">
                            <span id="editTitleCount">0</span>/30 characters
                        </small>
                    </div>

                    <!-- Description Input -->
                    <div class="form-group">
                        <label for="editPostDescription">
                            <span style="color: #667eea; font-weight: 700;">✍️</span>
                            Description
                            <span style="color: #f5576c; font-size: 0.9rem;">*</span>
                        </label>
                        <textarea id="editPostDescription" name="description" placeholder="Write post description..."
                            required maxlength="250"></textarea>
                        <small style="color: #999; display: block; margin-top: 0.3rem;">
                            <span id="editDescCount">0</span>/250 characters
                        </small>
                    </div>

                    <!-- Image Upload -->
                    <div class="form-group">
                        <label for="editPostImage">
                            <span style="color: #667eea; font-weight: 700;">🖼️</span>
                            Post Image (Optional)
                        </label>
                        <div class="file-upload" id="editFileUpload"
                            style="border: 2px dashed #667eea; border-radius: 12px; padding: 2rem; text-align: center; cursor: pointer; transition: all 0.3s; background: rgba(102, 126, 234, 0.05);">
                            <div style="font-size: 2.5rem; margin-bottom: 0.5rem;">📤</div>
                            <div style="color: #666; font-weight: 500;">Click to upload or drag and drop</div>
                            <div style="color: #999; font-size: 0.85rem; margin-top: 0.5rem;">PNG, JPG, GIF, WebP up to
                                5MB</div>
                            <input type="file" id="editPostImage" name="image" accept="image/*"
                                style="display: none;" />
                        </div>

                        <!-- Current Image Display -->
                        <div id="currentImagePreview" style="margin-top: 1.5rem; display: none;">
                            <p style="color: #999; font-size: 0.9rem; margin-bottom: 1rem;">📁 Current Image:</p>
                            <div style="position: relative; display: inline-block;">
                                <img id="currentImg" src="" alt="Current"
                                    style="width: 200px; height: 200px; object-fit: cover; border-radius: 15px; box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3); border: 3px solid white;">
                            </div>
                        </div>

                        <!-- New Image Preview -->
                        <div id="editImagePreview"
                            style="margin-top: 1.5rem; display: none; text-align: center; padding: 2rem; background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(240, 147, 251, 0.1) 100%); border: 2px solid #667eea; border-radius: 20px;">
                            <div style="position: relative; display: inline-block; margin-bottom: 1rem;">
                                <img id="editPreviewImg" src="" alt="Preview"
                                    style="width: 200px; height: 200px; object-fit: cover; border-radius: 15px; box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3); border: 3px solid white;">
                                <div
                                    style="position: absolute; top: -10px; right: -10px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; width: 40px; height: 40px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: bold; box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4); font-size: 1.2rem;">
                                    ✓</div>
                            </div>
                            <div style="margin-top: 1.5rem;">
                                <div id="editPreviewName"
                                    style="color: #333; font-size: 1rem; font-weight: 600; word-break: break-all; padding: 0.8rem 1rem; background: white; border-radius: 12px; margin-bottom: 1rem; border-left: 4px solid #667eea; box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);">
                                </div>
                                <button type="button" onclick="removeEditImage()"
                                    style="background: linear-gradient(135deg, #f5576c 0%, #f093fb 100%); color: white; border: none; padding: 0.6rem 1.5rem; border-radius: 20px; font-weight: 600; cursor: pointer; transition: all 0.3s; font-size: 0.9rem;">✕
                                    Remove Image</button>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-cancel" onclick="closeEditPostModal()">❌ Cancel</button>
                    <button type="submit" class="btn btn-primary">💾 Save Changes</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Delete Confirmation Modal -->
    <div class="modal" id="deleteConfirmModal">
        <div class="modal-content" style="max-width: 400px;">
            <div class="modal-header">
                <h2>Delete Confirmation</h2>
                <button class="close-btn" onclick="closeDeleteConfirm()">✕</button>
            </div>

            <div class="modal-body">
                <p id="deleteMessage" style="color: #666; font-size: 1.1rem; line-height: 1.6;"></p>
            </div>

            <div class="modal-footer">
                <button type="button" class="btn btn-cancel" onclick="closeDeleteConfirm()">Cancel</button>
                <button type="button" class="btn btn-danger" onclick="confirmDelete()">Delete</button>
            </div>
        </div>
    </div>

    <script>
        let editPostId = null;
        let deleteType = null;
        let deleteId = null;

        // Sidebar Navigation
        function showSection(section, event) {
            event.preventDefault();

            document.querySelectorAll('.section-content').forEach(el => {
                el.style.display = 'none';
            });

            document.querySelectorAll('.sidebar-menu a').forEach(el => {
                el.classList.remove('active');
            });

            const sectionId = section + '-section';
            const sectionEl = document.getElementById(sectionId);
            if (sectionEl) {
                sectionEl.style.display = 'block';
            }

            const titles = {
                'dashboard': 'Dashboard',
                'posts': 'Manage Posts',
                'users': 'Manage Users'
            };
            document.getElementById('page-title').textContent = titles[section] || 'Dashboard';

            event.target.classList.add('active');
        }

        // Edit Post Modal Functions
        function openEditPostModal(postId, postData) {
            editPostId = postId;
            document.getElementById('postIdInput').value = postId;
            document.getElementById('editPostForm').action = `/api/admin/${postId}?_method=PUT`;

            document.getElementById('editPostTitle').value = postData.title || '';
            document.getElementById('editPostDescription').value = postData.description || '';
            document.getElementById('editTitleCount').textContent = postData.title ? postData.title.length : 0;
            document.getElementById('editDescCount').textContent = postData.description ? postData.description.length : 0;

            // ✅ SHOW CURRENT IMAGE IF EXISTS
            if (postData.image) {
                const currentImg = document.getElementById('currentImg');
                currentImg.src = '/' + postData.image.replace(/\\/g, '/');
                document.getElementById('currentImagePreview').style.display = 'block';
            } else {
                document.getElementById('currentImagePreview').style.display = 'none';
            }

            // ✅ RESET NEW IMAGE PREVIEW
            document.getElementById('editImagePreview').style.display = 'none';
            document.getElementById('editPostImage').value = '';

            document.getElementById('editPostModal').classList.add('active');
        }

        function openEditPostModal(postId, postData) {
            editPostId = postId;
            document.getElementById('postIdInput').value = postId;
            document.getElementById('editPostForm').action = `/api/admin/${postId}?_method=PUT`;

            document.getElementById('editPostTitle').value = postData.title || '';
            document.getElementById('editPostDescription').value = postData.description || '';
            document.getElementById('editTitleCount').textContent = postData.title ? postData.title.length : 0;
            document.getElementById('editDescCount').textContent = postData.description ? postData.description.length : 0;

            // Show current image if exists
            if (postData.image) {
                const currentImg = document.getElementById('currentImg');
                currentImg.src = '/' + postData.image.replace(/\\/g, '/');
                document.getElementById('currentImagePreview').style.display = 'block';
            } else {
                document.getElementById('currentImagePreview').style.display = 'none';
            }

            // Reset new image preview
            document.getElementById('editImagePreview').style.display = 'none';
            document.getElementById('editPostImage').value = '';

            document.getElementById('editPostModal').classList.add('active');
        }

        // ADD THIS AFTER closeEditPostModal FUNCTION

        // Edit File Upload Handling
        const editFileUpload = document.getElementById('editFileUpload');
        const editFileInput = document.getElementById('editPostImage');
        const editImagePreview = document.getElementById('editImagePreview');
        const editPreviewImg = document.getElementById('editPreviewImg');
        const editPreviewName = document.getElementById('editPreviewName');

        if (editFileUpload) {
            editFileUpload.addEventListener('click', () => {
                editFileInput.click();
            });

            editFileUpload.addEventListener('dragover', (e) => {
                e.preventDefault();
                editFileUpload.style.background = 'rgba(102, 126, 234, 0.2)';
                editFileUpload.style.borderColor = '#764ba2';
            });

            editFileUpload.addEventListener('dragleave', () => {
                editFileUpload.style.background = 'rgba(102, 126, 234, 0.05)';
                editFileUpload.style.borderColor = '#667eea';
            });

            editFileUpload.addEventListener('drop', (e) => {
                e.preventDefault();
                editFileUpload.style.background = 'rgba(102, 126, 234, 0.05)';
                editFileUpload.style.borderColor = '#667eea';
                const files = e.dataTransfer.files;
                if (files.length > 0) {
                    editFileInput.files = files;
                    handleEditImageUpload();
                }
            });
        }

        if (editFileInput) {
            editFileInput.addEventListener('change', handleEditImageUpload);
        }

        function handleEditImageUpload() {
            const file = editFileInput.files[0];

            if (file) {
                if (file.size > 5 * 1024 * 1024) {
                    alert('File size must be less than 5MB');
                    editFileInput.value = '';
                    editImagePreview.style.display = 'none';
                    return;
                }

                const reader = new FileReader();
                reader.onload = (e) => {
                    editPreviewImg.src = e.target.result;
                    editPreviewName.textContent = '📁 ' + file.name + ' (' + (file.size / 1024).toFixed(2) + ' KB)';
                    editImagePreview.style.display = 'block';
                };
                reader.readAsDataURL(file);
            }
        }

        function removeEditImage() {
            editFileInput.value = '';
            editPreviewImg.src = '';
            editPreviewName.textContent = '';
            editImagePreview.style.display = 'none';
        }
        function closeEditPostModal() {
            editPostId = null;
            document.getElementById('editPostModal').classList.remove('active');
            document.getElementById('editPostForm').reset();
            document.getElementById('editTitleCount').textContent = '0';
            document.getElementById('editDescCount').textContent = '0';
        }

        // Character Counters
        document.getElementById('editPostTitle').addEventListener('input', function () {
            document.getElementById('editTitleCount').textContent = this.value.length;
        });

        document.getElementById('editPostDescription').addEventListener('input', function () {
            document.getElementById('editDescCount').textContent = this.value.length;
        });

        // Delete Confirmation
        function confirmDeletePost(postId, postTitle) {
            deleteType = 'post';
            deleteId = postId;
            document.getElementById('deleteMessage').innerHTML = `Are you sure you want to delete the post "<strong>${postTitle}</strong>"? This action cannot be undone.`;
            document.getElementById('deleteConfirmModal').classList.add('active');
        }

        function confirmDeleteUser(userId, userName) {
            deleteType = 'user';
            deleteId = userId;
            document.getElementById('deleteMessage').innerHTML = `Are you sure you want to delete the user "<strong>${userName}</strong>"? This action cannot be undone.`;
            document.getElementById('deleteConfirmModal').classList.add('active');
        }

        function confirmDeleteComment(commentId, authorName) {
            deleteType = 'comment';
            deleteId = commentId;
            document.getElementById('deleteMessage').innerHTML = `Are you sure you want to delete the comment by "<strong>${authorName}</strong>"? This action cannot be undone.`;
            document.getElementById('deleteConfirmModal').classList.add('active');
        }

        function closeDeleteConfirm() {
            deleteType = null;
            deleteId = null;
            document.getElementById('deleteConfirmModal').classList.remove('active');
        }

        function confirmDelete() {
            if (deleteId) {
                const form = document.createElement('form');
                form.method = 'POST';

                if (deleteType === 'post') {
                    form.action = `/api/admin/${deleteId}?_method=DELETE`;
                } else if (deleteType === 'user') {
                    form.action = `/api/admin/user/${deleteId}?_method=DELETE`;
                } else if (deleteType === 'comment') {
                    form.action = `/api/admin/comment/${deleteId}?_method=DELETE`;
                }

                document.body.appendChild(form);
                form.submit();
            }
        }

        // Close modal when clicking outside
        window.onclick = function (event) {
            const editModal = document.getElementById('editPostModal');
            const deleteModal = document.getElementById('deleteConfirmModal');

            if (event.target === editModal) {
                closeEditPostModal();
            }
            if (event.target === deleteModal) {
                closeDeleteConfirm();
            }
        }

        // Form Validation
        document.getElementById('editPostForm').addEventListener('submit', function (e) {
            const title = document.getElementById('editPostTitle').value.trim();
            const description = document.getElementById('editPostDescription').value.trim();

            if (!title || !description) {
                e.preventDefault();
                alert('Please fill in all required fields');
            }
        });
    </script>
</body>

</html>